# Changelog

## 2.1.0 - 2026-08-29

### 新增

- 升级换届粘贴名单支持两种行格式：仅「姓名」与「学号,姓名」（逗号 / 中文逗号 / 制表符 / 空格容错，前后端双重校验，空姓名、格式错误、同批重复显式报错）。
- 仅姓名导入：为目标班生成稳定临时学号 `TMP-{grade}-{class}-{name}`（同班幂等、跨班不冲突，不拿姓名当主键），导入后立即可在作业花名册与缺交录入中使用；预览仍按姓名给出上一年级候选，绝不自动合并身份。
- 正式学号补录：在同一输入框粘贴「学号,姓名」即把本班同名占位行事务性替换为正式学号，homework_record / special_record / student_note / student_alias 随迁，excluded / 座号 / 性别保留；学号已被占用、成绩库姓名不符、多条占位行、跨班同名等冲突整批拒绝并回滚。
- 学生检索与画像并入 roster-only 学生：只有花名册、尚无成绩的学生按当前班显示，成绩 / 名次 / 最近考试为「—」，可按姓名搜索，详情页安全展示姓名与已有作业 / 档案（不再 404）。
- 只读 MCP 服务端（`app/mcp_server.py`，Streamable HTTP、stateless JSON，挂载 `/mcp`）：供笔记本 Hermes 等远程 AI 客户端经公网 HTTPS 调用。`MCP_ENABLED` 默认关闭，关闭时不挂载、不导入 mcp SDK，现有应用行为不变；启用后经独立 Bearer Token（`MCP_BEARER_TOKEN`，缺失/弱/短于 32 字符直接启动失败）+ MCP SDK Host/Origin 防护对外。
- `chat/tools.py` 引入单一注册源 `TOOL_REGISTRY`（每项显式 `read_only`）：公开 `TOOLS` 改为其三键投影（结构、顺序、内容不变），MCP 目录自动取 `read_only: True` 条目，tools/call 一律经既有 `execute_tool()` 分发。
- `backend/tests/test_mcp.py`：MCP 协议、认证与注册表投影测试（固定 20 个只读工具的名称、顺序和元数据契约）。
- `Caddyfile` 增加 `/mcp` 与 `/mcp/*` → backend:8000 路由；文档补充 Hermes 配置与 NAS 部署说明。

### 修复

- 换届粘贴名单把单列姓名当学号写入、且行级 `class_num: null` 覆盖目标班级，导致产生 `grade=2、class_num/name` 为空的脏名册行的问题；现在所有写入严格按教师绑定的目标 grade+class（不一致返回 409）。
- 再次粘贴同名名单时自动收编旧版缺陷行（`student_id=姓名`、`class_num/name` 为空）：依赖数据随迁到正确学号后删除缺陷行；未粘贴的缺陷行与高一年级数据不受影响。
- 名册写入假成功计数（如「新增 0、更新 41、共 0」）：total 现为真实目标班行数，并新增「补学号 / 修复旧数据」计数与错误文案分流（alert + danger 色）；FastAPI 422 数组型 detail 不再渲染为 `[object Object]`。
- 已关联跨学年身份但尚无成绩的 roster-only 学生不再从当前班消失：花名册学号与旧成绩学号并入同一「人」，以高二学号为当前代表（`current_grade` / `class_num` = 高二目标班，旧学号进 `history`，可按姓名/临时学号搜索）；详情页年级集合并入合法花名册年级，顶层 `class_num` 取最高年级作用域，`has_cross_year` 口径同步。
- 「从成绩派生」建花名册复用同一占位替换事务：先按姓名建册的学生出分后自动换成正式学号并迁移作业 / 特殊记录 / 档案 / 身份别名，不再 merge 出第二条重复行（旧行为还会把座号 / 性别重置为空）。
- 正式学号导入统一冲突校验：无论是否命中占位行，都校验成绩库姓名、目标年级班级与已挂身份别名，不符整批拒绝（同名且作用域一致可安全接续）；旧缺陷行收编前比对两侧别名（不同 identity 整批拒绝，同 identity 收编且删除缺陷学号别名不留孤儿）；占位判定改为精确等于系统临时学号，任何以 `TMP-` 开头的真实学号不再被误替换。
- 学生列表 / 详情的 roster-only 作用域收敛：只纳入教师绑定的年级班级，他班 roster-only 学生列表不可见、详情 404；有成绩的跨学年画像行为不变。
- `test_mcp` 中失效的 git HEAD 基线辅助（exec 时 `TOOL_REGISTRY` 未定义）改为稳定的注册表投影契约：TOOLS 恰为 `TOOL_REGISTRY` 三键投影、20 个名称 / 顺序 / 只读元数据固定，不再依赖仓库状态。

## 2.0.0 - 2026-07-18

### 新增

- 面向班主任工作流的 Direction B 全站设计系统与响应式 App Shell。
- 全局“年级 + 已绑定行政班”作用域，以及加载、空数据、错误、首次使用和部分成功状态。
- 通用页面标题、指标卡、内容卡、筛选栏、数据表、移动成绩卡、图表面板、步骤向导和上传拖放组件。
- UI 契约测试与班级作用域 Provider 交互测试。

### 改进

- 重构仪表盘、考试分析、班级对比、学生画像、作业跟踪、成长档案、AI、上传、升级换届和备份交互。
- 考试成绩矩阵提供冻结列、可访问排序和完整移动成绩字段。
- 学生详情与 A4 一页纸按当前行政班校验，打印严格限定当前学生并保持单页。
- 作业录入、预警、相关性和花名册统一使用当前班级，保留请假、排除和同名隔离口径。
- AI 对话上下文与工具调用使用当前班级，并展示真实工具运行状态。
- 移动端交互目标统一为至少 44px。

### 修复

- 删除前端、作业后端和 AI 工具中的运行时固定 6 班回退。
- 修复快速切班时旧请求覆盖新班数据的竞态。
- 修复接口失败被显示为零指标或空记录的问题。
- 后端测试改用临时隔离数据库，不再读写真实用户数据。

### 兼容性

- 保留全部现有路由、API 路径、请求字段和响应结构。
- 保留高一与高二/高三成绩结构、段位阈值、缺考、身份接续、上传预览提交、作业和备份恢复口径。
- 不新增数据库表。
